# dependencies
node_modules/
/.pnp
.pnp.js

# testing
/coverage

# production
/build
/dist

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS generated files
Thumbs.db
.claude-flow/metrics/*
# Swarm memory/índices (contêm notas internas de auditoria) — nunca versionar
**/.swarm/
backend/.env

# Postman — arquivos voláteis do monitor (logs, dedup state)
postman/.monitor.log
postman/.monitor.last-run
postman/.monitor.last-alert
# Postman performance baselines — JSONs por data são regerados
postman/baselines/*.json
!postman/baselines/.gitkeep
# Postman perf-reports e security-reports (autocannon JSON, ZAP HTML/JSON)
postman/perf-reports/*.json
postman/security-reports/*.html
postman/security-reports/*.json
# Monitor opensign (debounce/log)
postman/.monitor-opensign.log
postman/.monitor-opensign.last-run
postman/.monitor-opensign.last-alert
# Jest
backend/coverage/
backend/.jest-cache/

# Read.ai OAuth tokens (sensitive)
backend/.secrets/

# Zoho OAuth — .env raiz + cache do access token + lock dirs (credenciais)
/.env
zohomail-authentication/.zoho-token
zohomail-authentication/.zoho-token.lock
zohomail-authentication/.zoho-token.lock.d/
zohomail-authentication/.zoho-token.tmp

# backend-sdk-claude — secrets, runtime state e dados pesados/sensíveis
backend-sdk-claude/.env
backend-sdk-claude/.env.bak-*
backend-sdk-claude/.mcp.json
backend-sdk-claude/LinkedIn/secrets/
# knowledge-base/ foi removida (99647d9); o processo antigo em memória ainda pode
# recriá-la até o próximo restart — não deixar o auto-commit ressuscitar no git
backend-sdk-claude/knowledge-base/
backend-sdk-claude/node_modules/
backend-sdk-claude/data/whatsapp-auth/
backend-sdk-claude/data/whatsapp-auth.bak-*/
backend-sdk-claude/data/whatsapp-inbound-media/
backend-sdk-claude/data/heygen-videos/
backend-sdk-claude/data/video-frames/
backend-sdk-claude/data/state.db
backend-sdk-claude/data/state.db-shm
backend-sdk-claude/data/state.db-wal
backend-sdk-claude/data/contacts.json.bak-*
# Hermes Agent SDK (upstream Nous Research, 524M) — referência local apenas
backend-sdk-claude/hermes-agent/

# Claude Code config local (rene init gera o conteúdo — não versionar)
.claude/
.claude-flow/

# Coverage em qualquer subdiretório
**/coverage/

# Logs em qualquer lugar
*.log

# Backups *.bak / *.bak-* (renomes/migrações antigas)
*.bak
*.bak-*

backend-sdk-claude/data/*
backend-sdk-claude/data/metrics.json
backend-sdk-claude/data/tasks.json
backend-sdk-claude/MazyOS
backend-sdk-claude/paperclip

.kite-passport/
backend-sdk-claude/bridge-hermazyo/Mazzeo-IA
hermes-webui

# Ambientes virtuais Python
.venv/
.venv-*/
**/.venv/
__pycache__/
*.pyc
